// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

// generator client {
//   provider = "prisma-client"
//   output   = "../generated/prisma"
// }

// datasource db {
//   provider = "postgresql"
//   url      = env("DATABASE_URL")
// }
// This is your Prisma schema file.
// Learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  // Connection URLs are no longer supported in schema files.
  // Move your database connection configuration to prisma.config.ts
  // (see https://pris.ly/d/config-datasource) and pass the adapter
  // or accelerateUrl when constructing PrismaClient.
}

// User model for Google OAuth and Email/Password Auth
model User {
  id        String    @id @default(cuid())
  googleId  String?   @unique // Google's unique profile ID (optional for email auth)
  email     String    @unique
  name      String
  password  String?   // Hashed password for email/password auth
  avatarUrl String?   // Optional profile picture
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt

  // --- E-commerce features to add later ---
  // cart      Cart?
  // wishlist  Wishlist?
  // orders    Order[]
}

// --- Add these models later for features ---
// model Product { ... }
// model Category { ... }
// model Cart { ... }
// model Wishlist { ... }
// model Order { ... }